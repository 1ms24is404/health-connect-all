version: '3.9'
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: health-connect-all:latest
    ports:
      - '8080:80'
    restart: unless-stopped
    environment:
      - NODE_ENV=production
